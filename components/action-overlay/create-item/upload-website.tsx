"use client";

import React, { useState } from "react";

const UploadWebsite = () => {
    const [uploadWebsite, setUploadWebsite] = useState(false);

    return <div className="bg-black absolute h-screen text-white">UploadWebsite</div>;
};

export default UploadWebsite;
